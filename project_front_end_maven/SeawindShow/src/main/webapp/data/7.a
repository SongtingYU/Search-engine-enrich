In your case, what you can do is: z = dict(x.items() + y.items()) This will, as you want it, put the final dict in b be properly overridden by the second (y) dict's value: >;>;>; x = {'a':1, 'b': 2} >;>;>; y = {'b':10, 'c': 11} >;>;>; z = dict(x.items() + y.items()) >;>;>; z {'a': 1, 'c': 11, 'b': 10} If you use Python 3, it is only a little more complicated. To create z: >;>;>; z = dict(list(x.items()) + list(y.items())) >;>;>; z {'a': 1, 'c': 11, 'b': 10} 